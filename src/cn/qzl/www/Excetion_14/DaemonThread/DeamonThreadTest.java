package cn.qzl.www.Excetion_14.DaemonThread;import java.time.LocalDate;/** * @Author qzl * @Description * 守护线程是为其他线程服务的线程； * * 所有非守护线程都执行完毕后，虚拟机退出； * * 守护线程不能持有需要关闭的资源（如打开文件等）。 * @Since 2020-05-04 18:30 */public class DeamonThreadTest extends Thread{    //如果这个线程不结束，JVM进程就无法结束    //护线程是指为其他线程服务的线程。在JVM中，所有非守护线程都执行完毕后，无论有没有守护线程，虚拟机都会自动退出。    //    //因此，JVM退出时，不必关心守护线程是否已结束。    //    //如何创建守护线程呢？方法和普通线程一样，只是在调用start()方法前，调用setDaemon(true)把该线程标记为守护线程：    @Override    public void run(){        while(true){            System.out.println(LocalDate.now());            try {                sleep(1000);            } catch (InterruptedException e) {                e.printStackTrace();            }        }    }    public static void main(String[] args) {        Thread t = new DeamonThread();        t.setDaemon(true);// t设置为守护线程        t.start();        Thread s = new DeamonThreadTest();        s.start();    }}